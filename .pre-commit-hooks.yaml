- id: metascrub
  name: metascrub (no metadata in committed files)
  description: Fail the commit if a staged PDF/Office/image/SVG still carries metadata.
  entry: metascrub clean --check --no-json-report --no-html-report
  language: python
  pass_filenames: true
  types_or: [pdf, file]
  files: '\.(pdf|docx|xlsx|pptx|odt|ods|odp|doc|xls|ppt|svg|jpe?g|png|tiff?|heic|webp)$'
